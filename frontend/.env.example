# LeftoverLens Frontend Environment Variables
# Copy this file to .env and fill in your values

# Backend API URL — leave empty for same-origin (when backend serves frontend)
# Set this during development or if deploying frontend separately
VITE_API_URL=http://localhost:3001
